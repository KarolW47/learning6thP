public interface Categories {

    //call question which shows to user, takes question from db
    private void questionToUser(){
        //question with randomized a,b,c,d answer options
    };

    //returns true if user answered correctly, takes user's answer
    private boolean answerCheck() {
        //compare with db correct answer, then if same -returns true, else false
        return true;
    };
}
